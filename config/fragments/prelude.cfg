% To load before a multiplicity structure: `false` is used by the negative
% premises throughout, and `nat` is needed by algebras whose carrier is
% numeric (common/defs/mult/graded.bel).

../../lib/prelude/logic.bel
../../lib/prelude/nat.bel
../../lib/prelude/nat_order.bel
